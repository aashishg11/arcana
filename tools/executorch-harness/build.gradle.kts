// Throwaway harness for Arcana Week 5 (ExecuTorch curriculum week).
// Deliberately lives OUTSIDE the Arcana repo: no Arcana app code ships this week.
plugins {
    id("com.android.application") version "9.2.0" apply false
}
